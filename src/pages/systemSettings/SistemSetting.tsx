import * as React from 'react'
import { ContextMenuSample } from './SistemSettingTable'

interface IPropTypes {
}

const SistemSetting = (props: IPropTypes) => {
  return (
    <ContextMenuSample
    />
  )
}

export default SistemSetting
